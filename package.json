{
  "name": "@pontis/webhook",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "deploy": "npm version minor && sh scripts/deploy.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
